An audio youtube player for the panel in KDE Plasma. Requires [yt-dlp](https://github.com/yt-dlp/yt-dlp)

# Installtion

Run these commands:

```sh
mkdir build
cd build
cmake ../ -DCMAKE_INSTALL_PREFIX=/usr
make
sudo make install
```

Then add it to your panel
